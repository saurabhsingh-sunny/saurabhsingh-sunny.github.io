project:
  type: website
  output-dir: _site

website:
  title: "Decision Science Lab"
  description: "Transforming ambiguity into rigorous decision logic."
  
  navbar:
    sticky: true
    left:
      - text: "Home"
        href: index.qmd
      - text: "Case Studies"
        href: reports.qmd
      - text: "About"
        href: about.qmd
    right:
      - icon: linkedin
        href: https://linkedin.com/
      - icon: github
        href: https://github.com/

format:
  html:
    theme: 
      light: cosmo
      dark: slate
    css: styles.css
    toc: true
    include-in-header:
      text: |
        <script src="https://cdn.jsdelivr.net/npm/mermaid/dist/mermaid.min.js"></script>
        <script>mermaid.initialize({startOnLoad:true});</script>
        <script>
        if (window.matchMedia && window.matchMedia('(prefers-color-scheme: dark)').matches) {
          // OS is dark, do nothing
        } else {
          if (localStorage.getItem("quarto-color-scheme") === null) {
            localStorage.setItem("quarto-color-scheme", "dark");
            document.documentElement.classList.add('quarto-dark');
          }
        }
        </script>